# Krishna Kunal — Personal Portfolio

A clean, responsive personal portfolio website built with semantic HTML, modern CSS, and a little JavaScript. Includes dark mode, accessible components, and a mobile-friendly layout.

## Quick start

1. **Download** this folder (or clone your repo) and open `index.html` in your browser.
2. Replace content with your details — search for `Krishna` in files.
3. Add your photo at `assets/avatar.svg` (or replace with a JPG/PNG and update the `<img>` tag).

## File structure

```
📂 portfolio-krishna
├── index.html
├── styles.css
├── script.js
└── assets/
    ├── avatar.svg
    └── Resume_krishna.pdf (optional copy)
```

## Customize

- **Colors & theme**: edit CSS variables at the top of `styles.css`.
- **Sections**: duplicate a `.card` in Projects for more projects.
- **Dark mode**: toggled via the moon button (saved in `localStorage`).

## Deploy to GitHub Pages

1. Create a new GitHub repo (e.g., `portfolio`).
2. Push these files to the repo's `main` branch.
3. In **Settings → Pages**, set **Branch** = `main` and **Folder** = `/ (root)`.
4. Wait 1–2 minutes, then your site will be live at `https://<your-username>.github.io/portfolio/`.

## License

This project is MIT licensed — do whatever you want, just keep the license.
